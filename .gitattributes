# Lua source code
*.lua text eol=lf

# JSON manifests — must be LF, no BOM (CC:Tweaked JSON parser requirement)
*.json text eol=lf

# NFP paint files (CC pixel art) — treat as text, keep LF
*.nfp text eol=lf

# CC precompiled assets — treat as binary
font binary
gothic binary
surface binary
